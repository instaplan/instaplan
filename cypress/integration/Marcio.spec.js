it("Should be able to type on search bar", () => {
    cy.get("input[name='filterTitles']")
    .click()
    .type('Zoo')
})

it("Should be able to type on search bar", () => {
    cy.get("input[name='filterTitles']")
    .click()
    .type('Lewisville')
})
it("Should be able to type on search bar", () => {
    cy.get("input[name='filterTitles']")
    .click()
    .type('Nighclub')
})

it("Should be able to type on search bar", () => {
    cy.get("input[name='filterTitles']")
    .click()
    .type('tomorrow')
})
it("Should be able to type on search bar", () => {
    cy.get("input[name='filterTitles']")
    .click()
    .type('tonight')
})